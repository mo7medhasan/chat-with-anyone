import React from 'react'
import Logo from './Logo'

function Header() {
  return (
    <header>
        <nav>
           <Logo/>

            {/* links */}
        </nav>
    </header>
  )
}

export default Header